# import * from './publicAchievement.graphql';

input CreateAchievementInput {
  title: String!
  description: String!
  days: Int!
  type: Type!
  published: Boolean!
}

input CreateCommentInput {
  body: String!
  achievement: String!
}

input CreateReplayInput {
  body: String!
  comment: String!
}

input FeedInput {
  skip: Int!
  limit: Int!
}
