# import PublicAchievement from '../achievement/publicAchievement.graphql';
# import PublicUser from '../user/publicUser.graphql';

type FeedAchievements implements PublicAchievement {
    author: PublicUser!
}

input SearchUsersInput {
    name: String!
}

input getUserInput {
    id: String!
}

input CompleteDayInput {
    achievement_id: String!
}

input userAchievementsInput {
    active: Boolean!
}
